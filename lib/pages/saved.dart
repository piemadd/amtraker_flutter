import 'package:flutter/material.dart';

class SavedTrains extends StatelessWidget {
  const SavedTrains({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Saved Trains',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
